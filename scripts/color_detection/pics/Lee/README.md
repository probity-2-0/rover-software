Photos from Lee's Experiments

The "original" is the original image plotted by the first plt.show()

The masked images are created in the iterations and in each iteration there is a range DEFINED BY the boundary tuple

This is file content for lee v2

The black mask is everything that could not make it into RGB(Not in specified range I statically hardcoded)
